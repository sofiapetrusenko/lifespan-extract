# Development tooling. Runtime dependencies live in requirements.txt (Phase 1+).
#
# Install into the project venv, never into a global or conda env:
#   python3.11 -m venv .venv
#   .venv/bin/pip install -r requirements-dev.txt

ruff
pytest

# jsonschema moved to requirements.txt in Phase 2: the extractor validates every
# record it produces, so it is a runtime dependency now, not a checking-tool one.
# The 4.18 floor is unchanged — earlier releases mishandle draft 2020-12
# unevaluatedProperties, which every claim wrapper in the schema relies on.
