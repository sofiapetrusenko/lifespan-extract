iter 1 | REQUIRED: 4 | ingest/{pubmed,biorxiv,db,http,cli,dedup,models,errors}.py + 99 tests; reviewer flagged PubmedBookArticle silent drop, cross-run preprint->publication duplicate, unvalidated `published` DOI, dead self-throttle
iter 2 | REQUIRED: 2 | fixed book-record drop, published-DOI validation, dead throttle; cross-run dedup documented as accepted limitation. Reviewer: efetch count check aborts on DeleteCitation; preprint->publication test does not hit the mechanism it claims to pin
iter 3 | REQUIRED: 0 | set-based PMID accounting w/ DeleteCitation, cross-run dedup tests split to hit real mechanism, password test off the socket -- APPROVE
iter 4 | REQUIRED: 0 | human-requested SUGGESTED follow-up: DEFAULT_RETRY_POLICY constants pinned, biorxiv non-string doi/published -> windowed ResponseFormatError, two overclaiming docstrings corrected
iter 1 | REQUIRED: 4 | extract/{schema,model,classify,extract,cli}.py + 100 tests; reviewer flagged unverified source_quote (fabricated quotes accepted), anthropic>=0.69 floor predates output_config (0.77.0), transport errors escape the per-paper handler and abort the batch, generated experiment_id cannot reproduce 15/26 gold ids

HANDOFF 2026-08-12 — Phase 2 incomplete, review loop not closed. Branch `phase-2-extraction`, 19 files staged (+3001/-3), nothing committed, `data/gold/` untouched; `.venv/bin/ruff check .` clean and `.venv/bin/pytest` green at 626. Iteration 1 built `extract/{errors,schema,model,classify,extract,cli}.py` with a Haiku-4.5 classifier gate and an Opus-5 structured-output extractor, and the fresh-context reviewer returned four REQUIRED items, all four addressed in iteration 2: (1) `source_quote` was never checked against the source text — a fabricated quote produced a schema-valid record — now `extract/schema.py::check_quotes_verbatim` does exact-then-whitespace-collapsed matching mirroring `scripts/check_gold.py`, called from `extract_record`, with `collapse_whitespace` duplicated rather than imported (to avoid depending on the gold tooling) and the two pinned together by a parity test; (2) the `anthropic>=0.69` floor predated `output_config`, which first shipped in 0.77.0, so the floor is now `>=0.77` with the reason in the comment; (3) `anthropic.APIError` escaped the per-paper handler and aborted the whole batch, now wrapped as `ModelCallError` at the `extract/model.py` boundary so failures are counted and logged and the run continues; (4) the generated `experiment_id` cannot reproduce 15 of 26 gold ids (human disambiguates semantically `-male`/`-low-dose`, generator numerically `-2`; human hand-normalises agent names, generator does not) — the generator was deliberately left following the schema's documented convention because choosing between the two is the human's call, and the divergence is instead pinned by a test parametrized over the real gold files plus a dated NOTES.md entry. I independently mutation-tested item 1 as orchestrator: disabling the `check_quotes_verbatim` call turns exactly three tests red and restoring it returns the suite to green, so that check is load-bearing rather than decorative. **The iteration-2 review has not run** — the reviewer dispatch was rejected at the tool-use layer for infrastructure reasons, not by any policy or finding, so the loop is stopped after one completed review round with four fixes unreviewed. Phase 2 is NOT done: per CLAUDE.md the definition of done requires a fresh-context review returning zero REQUIRED items, and no PR should be opened until that runs. Resume by re-dispatching the reviewer against `git diff --cached main` (a saved copy is at `scratchpad/phase2-iter2.diff`, 3148 lines) and continuing the loop from iteration 2. Still unverified for want of an `ANTHROPIC_API_KEY` and a `DATABASE_URL`: the live API round trip, whether the derived request schema is accepted by the structured-output endpoint, prompt quality on real papers, and therefore PLAN.md's Phase 2 "Done when" (20 unseen papers end-to-end). Open questions for the human: the `experiment_id` convention above; whether extracted records belong in `data/extracted/<schema_version>/` as JSON or in Postgres; and the classifier's deliberate bias toward answering true on ambiguous text.
