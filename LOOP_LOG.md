iter 1 | REQUIRED: 4 | ingest/{pubmed,biorxiv,db,http,cli,dedup,models,errors}.py + 99 tests; reviewer flagged PubmedBookArticle silent drop, cross-run preprint->publication duplicate, unvalidated `published` DOI, dead self-throttle
iter 2 | REQUIRED: 2 | fixed book-record drop, published-DOI validation, dead throttle; cross-run dedup documented as accepted limitation. Reviewer: efetch count check aborts on DeleteCitation; preprint->publication test does not hit the mechanism it claims to pin
iter 3 | REQUIRED: 0 | set-based PMID accounting w/ DeleteCitation, cross-run dedup tests split to hit real mechanism, password test off the socket -- APPROVE
iter 4 | REQUIRED: 0 | human-requested SUGGESTED follow-up: DEFAULT_RETRY_POLICY constants pinned, biorxiv non-string doi/published -> windowed ResponseFormatError, two overclaiming docstrings corrected
iter 1 | REQUIRED: 4 | extract/{schema,model,classify,extract,cli}.py + 100 tests; reviewer flagged unverified source_quote (fabricated quotes accepted), anthropic>=0.69 floor predates output_config (0.77.0), transport errors escape the per-paper handler and abort the batch, generated experiment_id cannot reproduce 15/26 gold ids

iter 2a | REQUIRED: 3 | iteration 2 split into two passes after the single full-diff dispatch failed twice at the tool-use layer; 2a = extraction core (extract/{schema,model,classify,extract,errors}.py + tests/test_{extract_schema,model,classify,extract}.py), cli/wiring/docs declared out of scope. Reviewer read all nine files in full, verified the installed anthropic 0.121.0 surface by hand, ran ruff + suite. REQUIRED: (1) extract/extract.py:145,158 — the title is in the prompt the model sees but not in the haystack passed to check_quotes_verbatim, so a claim quoting the title is rejected as a fabrication and kills the whole record; (2) extract/extract.py:201-206 — model-supplied `notes` is persisted into the record (schema-valid, no provenance, never scored) and model-supplied `experiment_id` is silently discarded, while the third pruned key `extracted_from` correctly raises; (3) extract/schema.py:207-211,231-243 — a whitespace-only source_quote passes check_provenance (truthy) and then check_quotes_verbatim (collapses to "", a substring of everything), vacuously satisfying the honesty invariant. 9 SUGGESTED. Reviewer reached the experiment_id gold divergence independently and deferred it to the human as correctly handled — not counted as a finding. VERDICT: CHANGES REQUIRED. No fixes dispatched; 2b runs next so the human sees one full picture.

iter 2b | REQUIRED: 3 | second half of the split iteration 2; 2b = interface and wiring (extract/{__init__,__main__,cli}.py, tests/test_extract_cli.py, tests/conftest.py, requirements{,-dev}.txt, .gitignore, NOTES.md), extraction core declared out of scope. Reviewer read all in-scope files in full, reproduced two failure modes on disk, ran ruff (clean) + pytest (626 passed). REQUIRED: (1) extract/cli.py:122,180 — Path.mkdir/write_text raise OSError, which neither the per-paper handler (ExtractError only) nor main (ExtractError, IngestError, ValueError) catches, so a filesystem error is a bare traceback that strands the rest of the batch and discards the summary, contradicting the file's own docstring; reproduced with `--out <a-file>` and with an unwritable out dir after 2 of 4 model calls were already paid for; (2) extract/cli.py:82-91,121-122 — `--out` is entirely unvalidated (type=Path, no normalisation, no guard), so `--out data/gold`, a `..` traversal or a symlink writes model output into human ground truth, and the written record is shape-identical to a gold file so Phase 3 would score the model against its own output; no default or internal path targets data/gold/, the hole is operator input only; (3) extract/cli.py:180 — the write is non-atomic and the file is the sole idempotence marker, so a Ctrl-C or short write leaves a truncated .json that every later run treats as already-extracted; also the necessary companion to fixing (1). 12 SUGGESTED, incl. two NOTES.md inaccuracies and a stale `pytest || [ $? -eq 5 ]` in ci.yml flagged but untouched per CLAUDE.md. VERDICT: CHANGES REQUIRED.

Iteration 2 totals: 6 REQUIRED (3 core + 3 wiring), zero overlap between passes, both passes independently ran ruff and the suite green. No fixes dispatched — findings handed to the human first, as instructed.

iter 2 fixes | 8 items | human ruled none of the six REQUIRED were accepted limitations. Implementer fixed all six plus two promoted SUGGESTED, in severity order with atomic writes (2b-3) before OSError handling (2b-1). cli.py: new `resolve_out_root` at the `run_extraction` chokepoint, resolving both sides and comparing case-folded path *components* (component-wise so `data/goldilocks` is not caught by a prefix match; case-folded because APFS opens `data/GOLD` as `data/gold`), raising ValueError so main exits 2 as invalid argument; `_write_record` via `tempfile.mkstemp(dir=path.parent)` + `os.replace` with `finally: unlink(missing_ok=True)` so Ctrl-C also cleans up; OSError added to the per-paper handler and to main's setup handler; module docstring corrected, it had promised behaviour the code lacked. extract.py: prompt built once and passed to both `call_structured` and `check_quotes_verbatim` so the two cannot drift; new `_refuse_code_assigned` driven by the existing CODE_ASSIGNED_* frozensets in schema.py (imported, not restated) so a volunteered `notes` or `experiment_id` raises exactly as `extracted_from` did. schema.py: `check_provenance` uses `_quotes_something` instead of truthiness. NOTES.md: two verified factual errors corrected — "no gold record uses a numeric suffix" -> "numeric *disambiguation* suffix", naming kenyon1993-celegans-daf-2 and lakowski1998-celegans-eat-2 as gene names; `metformin + rapamycin` -> `metformin plus rapamycin`, the literal value in strong2016.json. ci.yml: `pytest || [ $? -eq 5 ]` -> `pytest`, human-authorised as tightening a check, not making one pass. experiment_id generation deliberately unchanged; pinned divergence list untouched and still passing. Implementer mutation-tested all six fixes (revert -> target test red -> restore): 6/6 red, item 5 injecting KeyboardInterrupt rather than OSError so it is not propped up by item 6's handler. Orchestrator verified independently: ruff clean, 626 -> 646 tests green, `git status --porcelain data/gold/` empty, nothing staged. Path-guard tests run against the real GOLD_ROOT for the four spellings (writes nothing even with the guard removed) and monkeypatch a stand-in for the two integration refusals, so mutation testing could not write into the real gold set.

iter 3a | REQUIRED: 1 | extraction core, fresh context, same split as 2a. All three iteration-2 fixes in scope verified correct by the reviewer's own mutation testing in a scratch copy: prompt-built-once (reverting to `text` fails the title test, and the negative side is pinned too), `_refuse_code_assigned` (complete at both experiment and claim level, with a drift guard test, and a key at an uncovered position still caught by `additionalProperties: false`), `_quotes_something` (closed at the right layer). Reviewer also re-confirmed the anthropic 0.121.0 request surface. NEW REQUIRED, not a regression from the fixes: extract/extract.py:352 `_ascii` deletes non-ASCII characters instead of transliterating or replacing them with a separator, so `17-α-estradiol` and `17-β-estradiol` both slug to `17-estradiol`, `α-`/`γ-tocopherol` both to `tocopherol`, and `restriction—long` loses its word boundary. Orchestrator reproduced all of these directly. This is a correctness bug independent of the reserved convention question — dropping α is wrong under every candidate convention. It bites a PLAN.md-named hard case: data/gold/strong2016.json carries both `17‐α‐estradiol` and `17‐β‐estradiol` (U+2010) in its source quotes, and the schema's own documented example id is `strong2016-mmusculus-17-alpha-estradiol`, which the generator cannot produce — so the code disagrees with the example it claims to follow, and `_ID_PATTERN` matches the lossy output. Why it survived three iterations: `test_generated_ids_match_gold_except_where_pinned` never sees it because every gold `intervention.agent.value` is already hand-normalised to ASCII, and `tests/test_extract.py:309 test_non_ascii_agents_still_produce_a_valid_identifier` asserts the lossy `17-estradiol` as the desired output — decorative coverage that blesses the defect. 6 SUGGESTED, incl. the collapsed haystack now joining title to abstract so a straddling quote verifies. VERDICT: CHANGES REQUIRED.

iter 3b | REQUIRED: 0 — APPROVE | interface and wiring, fresh context, same split as 2b. Reviewer reverted each of the three iteration-2 fixes in a scratchpad copy of the repo: dropping the guard call -> 2 failures, replacing the guard body with a naive unresolved `str.startswith` -> 5 failures (parent segment, case variant, symlink, resolved return, goldilocks), `_write_record` -> `write_text` -> 2 failures, narrowing the two except clauses -> 2 failures. All three load-bearing, and the guard pinned from both ends (function by the spelling tests, call site by the two run-level tests). Probed `resolve_out_root` against 13 spellings on APFS: refuses data/gold, trailing slash, non-existent descendant, non-existent + case variant, `..` traversal, symlink-to-gold, non-existent path under a symlink, symlinked *parent*, `./data/./gold`, doubled separators; correctly allows data/goldilocks and data/gold_backup. Confirmed `Path.resolve()` on a non-existent path uses realpath(strict=False) kernel semantics, so there is no `..`-after-symlink escape. Chokepoint claim verified by grepping every write primitive in extract/: exactly two sites, both strictly downstream of the guard; `record_path` is public but pure. No test can write into the real data/gold/ — spelling tests call the pure function, run-level tests redirect GOLD_ROOT to a tmp stand-in, and the constant is pinned to the real path so the redirect cannot rot. Both NOTES.md corrections verified against the gold set (only two ids match `-\d+$`, both gene names; the literal value is `metformin plus rapamycin`; 11/26 and 15 divergences across 7 files all check out). ci.yml diff is exactly the escape removal, every other line byte-identical, strictly tightening. 9 SUGGESTED, incl. a systemic OSError now costing 20x model calls instead of aborting after one, and no fsync before os.replace. VERDICT: APPROVE.

Iteration 3 totals: 1 REQUIRED (3a) + 0 (3b). The wiring half is clean; the extraction half has one new correctness defect that predates the iteration-2 fixes and was masked by a decorative test. Not fixed — handed to the human.

iter 3 fixes | 2 items | human ruled both in. (1) extract/extract.py `_ascii` rewritten to be information-preserving: NFKD -> drop combining marks -> transliterate -> remaining non-ASCII becomes the separator -> collapse runs and strip. `_TRANSLITERATED` covers the full lowercase Greek alphabet (both sigmas) with uppercase derived, filtered to `len(letter.upper()) == 1` because "ß".upper() is two characters. Orchestrator verified: `17-α-estradiol` -> `17-alpha-estradiol` and `17-β-estradiol` -> `17-beta-estradiol` (matching the schema's documented example at experiment.schema.json:207), both U+2010 spellings converge on the same ids, `α-`/`γ-tocopherol` no longer collide, `restriction—long` keeps its boundary, and accented Latin still folds (café -> cafe, and the explicit combining-mark drop is what preserves that — without it NFKD would leave `e-`). Implementer flagged a deliberate scope judgment: 8 Latin entries (æ ø œ ß đ ð ł þ) included alongside Greek because they have no NFKD decomposition and would otherwise have become separators, turning `Søren` into `s-ren2016-…` — a new wrong id introduced by the fix rather than a pre-existing one. Verified `Søren` -> `soren`. The decorative test at test_extract.py:309, which asserted the defective `17-estradiol` as expected, was deleted and replaced by six tests. (2) extract/cli.py `_check_writable` called immediately after mkdir, probing with the same `tempfile.mkstemp` the real write uses rather than an os.access approximation, raising ExtractError before any model call; per-paper OSErrors still reach the per-paper handler unchanged. Mutation-tested: reverting `_ascii` turns 9 tests red, reverting the probe call turns the read-only test red on the stub's "unexpected extra model call" — the wasted-call condition itself, not a generic error. Implementer noted its first draft of the pattern-conformance test was insensitive to the revert (checked only `_ID_PATTERN`, which the broken output also satisfies) and strengthened it before re-running. Gold guard rail confirmed: ids regenerated for all 26 gold experiments under both old and new `_ascii` in-process, zero differences; KNOWN_ID_DIVERGENCES untouched and absent from the diff; no gold organism/species/agent value is non-ASCII. Orchestrator verified: ruff clean, 646 -> 656 tests green, `git status --porcelain data/gold/` empty, nothing staged. experiment_id convention unchanged.

iter 4a | REQUIRED: 3 | extraction core, fresh context, same split. Reviewer confirmed the iteration-3 `_ascii` fix is genuinely pinned — reverting the transliteration turns 9 cases red, and the pattern-conformance test is NOT the `_ID_PATTERN`-only decorative kind (it asserts the expected slug first). Checklist items 1-5 and 7 all clean. But three new findings, all in the same function, all orchestrator-reproduced. (1) extract/extract.py:423 — the trailing `.strip(_SEPARATOR)` deletes a *leading* untransliterable character, contradicting the function's own docstring ("Deleting a character is the one thing this must not do"). `Δ9-tetrahydrocannabinol` (U+0394) -> `delta9-…` but `∆9-tetrahydrocannabinol` (U+2206 INCREMENT) -> `9-tetrahydrocannabinol`, which collides outright with a literal `9-tetrahydrocannabinol`. Two Unicode spellings of one compound produce two ids, and a distinguishing leading mark is silently dropped — the iteration-3 defect in a new position, at a boundary the U+2010 test does not reach. U+2206/U+0394 is a common confusable pair in chemistry text and Δ9/Δ6 desaturases are a real C. elegans longevity intervention. Not convention territory: wrong under every candidate convention. Whack-a-mole warning: `ı`, `ə`, `ħ`, `ẞ` all behave the same at a boundary (`_slug("ə-test") == "test"`). (2) extract/extract.py:116-130 — 39 of 64 transliteration entries are dead to the suite. Reviewer deleted the whole 8-entry Latin block AND the 31-entry uppercase-Greek comprehension in a scratch copy: 656 still passed, nothing noticed. Orchestrator confirmed by grep: zero occurrences of ø æ ß đ ð ł þ œ or any uppercase Greek anywhere in tests/. The declared `Søren` rationale has no test behind it, and the subtle `len(letter.upper()) == 1` filter is unexercised. Same hazard as iteration 3 with the assertion missing entirely. (3) extract/extract.py:107-109,116-117 — the Latin set does not satisfy the membership rule its own comment states ("base letters in their own right", "the ones that appear in author names"): `ı ə ħ ŋ ĸ ŧ ẞ` all meet it and are absent, so `Yıldız` -> `y-ld-z` and `Işık` -> `is-k`, the exact `s-ren2016-…` outcome the table was added to prevent, and it shreds the first-author segment into three hyphen pieces. Turkish surnames are commoner in PubMed than Danish ø, and ingest/pubmed.py takes LastName from XML unnormalised. Reviewer ruled the Latin block itself justified and NOT concealed scope creep — a direct consequence of switching the fallback from delete to separator — it is the execution that fails. 3 SUGGESTED, incl. a well-argued case that the `10 µM` -> `10-mum` disposition was filed under the reserved convention question when it is entirely internal to `_ascii` (NFKD folds U+00B5 onto Greek mu before the table is consulted), and that `_experiment_id`'s `if not all(parts)` guard cannot catch an empty author slug because the year keeps parts[0] truthy. VERDICT: CHANGES REQUIRED.

iter 4b | REQUIRED: 0 — APPROVE (second consecutive) | interface and wiring, fresh context, same split. Reviewer re-reviewed the whole scope rather than leaning on 3b's approve, and mutation-tested the new probe with a pytest plugin loaded from the scratchpad (no repo files touched): removing `_check_writable` -> exactly the read-only test fails, on the stub's "unexpected extra model call to 'claude-haiku-4-5'", i.e. the run does reach a paid call without the probe; making the probe leave its temp file -> 2 tests fail, so litter on the *success* path is pinned because those tests assert on iterdir() not glob("*.json"); stripping `_write_record`'s finally -> same 2 fail, atomicity cleanup still pinned. Confirmed the probe runs after `resolve_out_root`, passes `dir=out_dir` and unlinks exactly the name mkstemp returned, so it cannot write outside the guarded path; all three write sites in cli.py are downstream of the guard. TOCTOU window exists but is a strict improvement — a directory going unwritable after the probe still lands in the per-paper handler, counted and non-fatal. Verified `make_client()` performs no network call, so the message's "no model call was made" is literally true. Verified an injected AssertionError propagates out of main rather than being absorbed as a paper failure. 6 SUGGESTED, two worth noting: `_check_writable`'s unlink is not in a try/finally unlike `_write_record`, whose docstring argues that exact hazard; and `resolve_out_root` guards `out_root` while the write root is `resolve_out_root(out_root) / version` where version comes from `schema_version`, which pins no shape — not exploitable today since the schema is repo-controlled and reads "0.4.0", but the chokepoint claim is strictly about the parent, not the write root. VERDICT: APPROVE.

Iteration 4 totals: 3 REQUIRED (4a) + 0 (4b). Wiring half approved twice consecutively. All three remaining items are in one function, extract/extract.py::_ascii. Loop is at iteration 4 of the 5 cap.

LOOP CAP EXTENDED 5 -> 7, Phase 2 only, by the human on 2026-08-12. Rationale, in the human's words: iterations 3 and 4 were successive patches to one function against one class of defect, not independent review rounds. CLAUDE.md's cap of 5 stands unchanged for every other phase; this extension is scoped to Phase 2 and recorded here rather than applied silently or by editing CLAUDE.md. Accompanying decision: `_ascii` is to be resolved by design, not by another table patch, since three rounds of entry-by-entry patching each produced a fresh finding in the same function.

iter 4 fixes | 5 items | human's design decision applied, `_ascii` resolved by design rather than a fourth table patch. (1) New `_CONFUSABLES` applied as the first step, before NFKD: U+2206->U+0394, U+00B5->U+03BC, and seven dashes (U+2010/2011/2012/2013/2014/2015/2212)->ASCII hyphen, so the table sees one canonical spelling per character. (2) Separator provenance carried as a mark *inside* the string rather than a flag beside it: `_ascii` now emits ASCII as-is, `_SEPARATOR` for characters that ARE punctuation/spacing (Unicode P*/Z*/C* plus ASCII controls), and `_SUBSTITUTE` (\x1a) for a character with no reading; a new `_split` is the only place that knows the difference — interior gaps always collapse, an END gap survives iff it contains `_SUBSTITUTE`. The old `.strip("-")` is gone from BOTH `_slug` and `_binomial_slug`; the leading strip in `_slug` was the second half of the iter-4 bug and would have re-deleted whatever `_ascii` preserved. (3) `_experiment_id` takes an `allocated` registry and raises the new `ExperimentIdCollisionError` (extract/errors.py) naming both source pairs and the shared id; the same pair still takes -2/-3. (4) µ canonicalises onto Greek mu and reads as `mu` — slug text unchanged, but `10 µM` and `10 μM` are now provably one thing; implementer's reasoning is that `micro` guesses wrong for µ-opioid/µ-calpain and a one-letter `u` is exactly the reading that can merge a Greek-lettered agent with an unrelated ASCII spelling. (5) Author guard now tests `_slug(paper.first_author)` on its own, and the parts check became `all(_WORD.search(part) ...)` because a slug can be a truthy "-" that names nothing. Orchestrator verified: `∆9-` and `Δ9-` both -> `delta9-tetrahydrocannabinol`, neither colliding with bare `9-`; `Yıldız`->`yildiz`, `Işık`->`isik`, `ə-test`->`e-test`; punctuation and whitespace still trim (`“rapamycin”` and `  rapamycin  ` -> `rapamycin`). ruff clean, 656 -> 917 tests green, gold untouched, nothing staged. Dead-table problem closed from both directions: parametrizing over the table catches a wrong entry but not a deleted one (deleting the entry deletes its case), so the table is additionally pinned by two coverage tests whose universe is derived from Unicode rather than from the table, plus a structural test that no entry is keyed on a character the lookup can never see — the micro-sign trap made unrepeatable. Implementer swept all 39 entries programmatically: every single deletion turns at least one test red. 14 mutations tested, all red, incl. restoring the iter-4 trim bug and the over-correction in the other direction. ONE HONEST GAP, self-disclosed: deleting µ->μ from `_CONFUSABLES` turns nothing red, because NFKD performs the identical fold one step later — the entry is declarative and cannot be made load-bearing; kept because it documents why the transliteration table has no micro-sign entry. DEVIATION TO FLAG: the human specified the collision key as `intervention.agent.value`; the implementer keyed it on the `(organism, agent)` source pair, arguing the numeric tail is licensed for the pair so the pair is what the guard should compare, and that it is strictly stronger (also catches `S. cerevisiae`/`S cerevisiae` merging). Fires in every case the human's wording describes. One tuple to revert. Gold guard rail: all 26 ids regenerated in-process before and after, zero differences; KNOWN_ID_DIVERGENCES untouched.

iter 5a | REQUIRED: 1 | extraction core, fresh context, same split. NOTE: the safety classifier was unavailable for this pass, so the orchestrator independently reproduced the finding rather than accepting it. Reviewer ran 84 single-entry table deletions plus 41 targeted behavioural mutations in a scratch copy. The design decision's logic is confirmed correct — the reviewer could not construct an input where `_ascii`/`_split`/`_experiment_id` delete a character or silently merge two compounds; checklist items 1-5 and 7 all clean; the behavioural mutations that matter all fail loudly (reinstating `.strip("-")` in `_slug` 3 red, keying the guard on agent alone 3 red, removing the guard 3 red, reverting the parts check 1 red, removing the author guard 1 red, `_ascii` deleting unreadable characters 3 red, disabling `_CONFUSABLES` 2 red). 82 of 84 table-entry deletions turn a test red, so the dead-table problem really is closed. THE ONE REQUIRED IS COVERAGE, NOT BEHAVIOUR: extract/extract.py:507 `_binomial_slug` — the human's design item 2 removed `.strip("-")` from BOTH `_slug` and `_binomial_slug`, but only the `_slug` half is pinned. Orchestrator independently reproduced: reinstating the iteration-4 full strip in `_binomial_slug` leaves all 917 tests passing. Also confirmed by grep that no test references `_binomial_slug`, `_organism_identity` or `_split` at all. Failure scenario: `species = "S. cerevisiaeЯ"` currently renders `scerevisiae-`, refused by `_ID_PATTERN`; with the strip back it silently becomes the clean species' id, a character deleted — the exact iteration-4 defect, on the other call site. Within one paper the collision guard catches it, but across papers nothing does, and Phase 3 aligns gold on these ids. 5 SUGGESTED, incl. `_INCIDENTAL_CATEGORIES` where only the `P` is pinned (so a non-ASCII space or soft hyphen at a boundary is untested either way), one further undisclosed dead entry `_TRANSLITERATIONS["Ə"]` exposing that `Ə` sits outside both derived universes, and a case where `organism="other"` with null species still admits a numeric tail between two different organisms — deliberately NOT called REQUIRED because the records carry identical organism evidence so the code has no basis to distinguish them; the honest fix is upstream (require `species` when `organism == "other"`) and is flagged as a Phase 3 input. Reviewer AGREED with the implementer's `(organism, agent)` keying deviation and confirmed it is enforced, not merely asserted (switching to agent-only turns 3 tests red); it raises on a strict superset of the human's wording. Reviewer endorsed the µ disposition. VERDICT: CHANGES REQUIRED.

iter 5b | REQUIRED: 3 | interface and wiring, fresh context, same split — breaking a two-pass approve streak, and all three are PRE-EXISTING defects that passes 3b and 4b missed rather than regressions from this round. NOTE: the safety classifier was unavailable for this pass too, so the orchestrator independently reproduced all three. (1) extract/cli.py:121-123 — `main`'s bare `except ValueError` assumes `resolve_out_root` is the only source, but UnicodeError/UnicodeEncodeError subclass ValueError and `_write_record` encodes model-derived text with `ensure_ascii=False`; a model payload with a lone surrogate ("\ud800", which json.loads accepts and yields as a str) raises UnicodeEncodeError inside the write. It is neither ExtractError nor OSError so the per-paper handler misses it too. Reviewer reproduced end-to-end: exit 2, no FAIL line naming the paper, no summary, second paper never attempted — every guarantee in the module's own docstring broken at once, and exit 2 is the code this CLI reserves for operator argument errors. Orchestrator confirmed each link: isinstance(UnicodeEncodeError, ValueError) is True, and cli.py:293 does use ensure_ascii=False. (2) extract/cli.py:293 — `json.dumps` defaults to allow_nan=True and `extract/model.py:135` calls `json.loads` with no `parse_constant`, so NaN/Infinity/-Infinity pass through; jsonschema classifies float('nan') as a valid `number`, so validation passes too. Orchestrator confirmed all three links directly. A bare `NaN` token lands on disk, exit 0, reported `ok`. Not JSON: JSON.parse throws for Phase 4's UI, serde rejects it, and jq silently rewrites it to null — turning a bogus number into a fabricated absence. Worst part: the file's existence is the only idempotence marker, so the paper is permanently marked extracted and no re-run replaces it. Reviewer warns the tempting local fix (allow_nan=False) makes finding 1 worse, since it raises ValueError; fix (1) first or reject at parse_payload. (3) tests/test_extract_cli.py:204 `test_the_limit_counts_attempts_not_rows` does not test what it is named for — the fixture stores three papers all with abstracts and none already extracted, so "attempts" and "rows" are the same number and the test cannot distinguish them. Orchestrator reproduced the reviewer's mutation: making BOTH skip branches consume the limit, inverting the documented contract outright, leaves all 917 tests green. The semantics are written into `build_parser`'s help text and `_papers`' docstring, and under the inverted behaviour `--limit 20` against a DB whose first 20 rows are already extracted processes zero papers and prints "nothing to do" forever. This is the third decorative test caught in this codebase. 5 SUGGESTED, incl. the package surface now re-exporting 4 of 6 error types (ModelCallError and the new ExperimentIdCollisionError both missing, second consecutive round of drift, argues for making it structural) and records inheriting mkstemp's 0600 where write_text gave 0644. Reviewer verified ExperimentIdCollisionError does land correctly in the per-paper handler — counted, logged, run continues, exit 1 — but only via the generic branch, with no test pinning it. All previously cleared items re-verified: guard, chokepoint, probe, atomicity, OSError handlers; 14 mutations run, 12 red; conftest earns its keep (making claim() attach a quote to absent values turns 55 tests red). VERDICT: CHANGES REQUIRED.

Iteration 5 totals: 4 REQUIRED (1 in 5a, 3 in 5b). The design fix to `_ascii` is behaviourally sound — 5a could not construct an input that deletes a character or merges two compounds — and its one finding is a coverage hole on the second call site. 5b's three are older defects surfaced by a harder look, not new breakage. Loop at iteration 5 of the extended cap of 7.

iter 5 fixes | 6 items | all four iteration-5 REQUIRED plus a promoted item and a documentation task, in the human's mandated order. (1) `resolve_out_root` now raises the new `OutputPathError(ExtractError)` instead of a bare ValueError, and main's `except ValueError` catch-all is GONE entirely — matched ahead of the (ExtractError, IngestError, OSError) clause so exit 2 still means operator typo. `_write_record` gained two typed re-raises for encoding failures, classified as `RecordValidationError` not `ModelResponseError` on the reasoning that a lone surrogate parses, validates against the schema, and is still unwritable — retrying the model would not help, which is exactly what separates the two types. Both raise before os.replace, so no file appears and the paper stays pending. (2) `parse_payload` now passes `parse_constant=_reject_constant`; the implementer added `parse_float=_finite_float` beyond spec after noticing the hook alone is bypassable — `{"x": 1e400}` is syntactically valid JSON that float() overflows to inf, never touching parse_constant. Orchestrator confirmed all three doors now refused: NaN, Infinity, and 1e400. `allow_nan=False` added at the write as second-line defence. (3) `_binomial_slug`, `_organism_identity` and `_split` now covered directly, none having been referenced by any test before. Orchestrator re-ran the exact 5a mutation: reinstating the iteration-4 full strip now turns 12 tests red where it previously left all 917 green. Implementer also tested leading-only (5 red) and trailing-only (7 red) variants. (4) `test_the_limit_counts_attempts_not_rows` rewritten against a 5-paper fixture ordered by dedup_key — already-extracted, abstract-less, then three fresh — asserting considered == 2, (already_extracted, no_abstract) == (1, 1), the exact set of output filenames so it pins WHICH papers ran, and its own ordering premise. All three inversions now red (either branch: considered 1 != 2; both: 0 != 2). (5) All seven public error types re-exported, with two tests in the new tests/test_extract_errors.py deriving the expectation from `extract.errors` rather than a hand-written list — a list being the same failure one level up. Orchestrator confirmed parity holds with an empty missing-set. (6) NOTES.md "Test honesty" section written, recording all three decorative tests, the `_binomial_slug` near-miss, the standing rule that a test asserting the current output of a function is not coverage of that function's contract, the common cause (each written by reading the implementation rather than the contract), and the two things that actually caught them (mutation, and deriving a test's universe from something other than the thing under test). Orchestrator verified: ruff clean, 917 -> 960 tests green, gold untouched, nothing staged. Gold guard rail: 10 files, 26 experiments, before/after IDENTICAL, KNOWN_ID_DIVERGENCES untouched — item 3 was tests only, the generator was not modified. Implementer flagged out of scope: `_papers` holds a Session open across every model call (fixing it changes the selection semantics item 4 just pinned), and `record_path`'s `.strip("-")` is the same trim as items 3/4 but is CORRECT there because the appended 8-char SHA-256 digest is what makes the mapping injective — noted so a future reader does not "fix" it by symmetry.

iter 6a | REQUIRED: 4 | extraction core, fresh context, same split. Confirmed the iteration-5 repairs hold exactly as claimed: reinstating the strip on `_binomial_slug` -> 12 red, leading-only -> 5, trailing-only -> 7; `_split` and `_organism_identity` genuinely pinned across six mutations; transliteration table still has no dead entry; both new json.loads hooks load-bearing and independently so (dropping parse_float -> 2 red, parse_constant -> 6 red), and the refusal correctly fires on the REPAIRED candidate and is retried once before raising. Checklist 1-3 and 7 clean, with every neutered guard turning tests red. Four new findings. (1) extract/model.py:176 — `json.loads` overrides parse_constant and parse_float but leaves `parse_int` as the default `int`, which on Python 3.11+ refuses a literal over 4300 digits with a plain ValueError; parse_payload catches only JSONDecodeError and _NonFiniteNumber, so it escapes the package hierarchy entirely. Orchestrator reproduced: a 5000-digit value escapes as a bare ValueError. THIS IS NOW STRICTLY WORSE THAN BEFORE — last round's 5b-1 fix removed main's `except ValueError` catch-all, so where this would previously have been a mislabelled exit 2 it is now a raw traceback; neither the per-paper handler nor main catches it, and a 20-paper run dies at whichever paper triggers it. Breaks three promises at once: call_structured's docstring guarantee that a caller can catch one type, the single retry (1 request, not 2), and the windowed excerpt. A repetition loop emitting digits is a real model failure mode and 4300 digits sits well inside the 16000-token cap. (2) tests/test_model.py:89-133 — every excerpt test uses a payload under 40 characters, but `ingest.errors.excerpt` has DEFAULT_RADIUS 200, so `position=None` (first 400 chars) and a centred window are byte-identical; both `position=` arguments can be set to None with all 960 green. The token assertion passes regardless because the reason string interpolates the token itself. PLAN.md's invariant is "unrepairable payloads raise with a windowed excerpt" and the centring is the entire diagnostic value — precisely what this round added. (3) tests/test_classify.py:51 — the "cheap model" half of the test's name is tautological: it asserts `request["model"] == CLASSIFIER_MODEL`, pinning the wiring but not the constant's value. Orchestrator reproduced: setting CLASSIFIER_MODEL to claude-opus-5 leaves all 960 green — PLAN.md's cost cascade can be collapsed entirely with the suite passing, and the reviewer confirms swapping both constants or setting them equal is also green. Same shape as the decorative CLI test caught in 5b. (4) extract/schema.py:308,367 `_inherited_description` — both branches carry comments naming the bug they prevent and both can be deleted with 960 green; live effect is that every bare-$ref claim key loses its guidance, or that `organism` and `sex` get relabelled with the provenance boilerplate, in the schema sent to the model. Lowest severity (degrades the prompt rather than corrupting a record) but a silent prompt regression surfaces only as a worse Phase 3 number. 5 SUGGESTED, incl. `_FENCE_RE` being fully redundant (deleting the .sub leaves 960 green) and `candidate.find(exc.token)` centring on the first textual occurrence, so a payload with "NaN mutant" before a real bare NaN windows the innocent string. VERDICT: CHANGES REQUIRED.

iter 6b | REQUIRED: 0 — APPROVE | interface and wiring, fresh context, same split. Reviewer ran 14 mutations, ALL killed: both --limit skip branches, reversed paper order, gold guard disabled and gold guard weakened to a string prefix, allow_nan removed, UnicodeEncodeError re-raise removed, writing the surrogate through with errors="surrogatepass", raising AFTER os.replace, finally-unlink removed, probe file not unlinked, probe call removed, main's except-clause order swapped, per-paper OSError dropped, and a new error class added to errors.py but exported nowhere (checked in all three shapes). Confirmed the rewritten --limit test is no longer decorative — it asserts its own ordering premise, the split counts, and the exact output filename set, and queues exactly two attempts' worth of stub responses so a third attempt raises from StubMessages rather than passing quietly. Confirmed the export-parity test genuinely fails on an unexported class. All previously cleared items re-verified after the exception-type change: the guard survived it (OutputPathError subclasses ExtractError, and the clause ordering in main is what preserves exit 2, mutation-confirmed), probe still runs after mkdir and before the first model call, and both new raise sites in `_write_record` precede os.replace so no partial file survives. NOTES.md fact-checked exhaustively against the repo — 26 experiments across 10 files, 15 divergence pairs, the gene-name `-2` claim, all four agent-normalisation examples, both schema quotes verbatim, and case 3 of the new "Test honesty" section matching the HEAD version of the test exactly; case 2's arithmetic is internally consistent and reads as the dated historical record it is. ON THE HANDLER-WIDTH QUESTION the orchestrator posed: reviewer judged extract/cli.py CORRECT AS WRITTEN and explicitly declined to require a backstop. Reasoning: removing main's except ValueError was strictly an improvement (a data failure was being labelled an operator typo); a replacement backstop cannot resume the batch so it buys nothing and would hide the traceback a maintainer needs; and widening the per-paper handler to `Exception` would be the wrong fix, converting a future refactor's TypeError/KeyError into counted FAIL lines indistinguishable from bad model output — "20 considered, 0 extracted, 20 failed" reading as a bad batch of papers, which is the failure-absorption the checklist forbids. The correct invariant is the one cli.py already depends on: everything originating in untrusted model output leaves the model layer as an ExtractError. parse_payload violates it today; fix it there (6a's item 1) and the ValueError becomes a ModelResponseError the existing handler counts. Reviewer explicitly declined to double-report it. 5 SUGGESTED, incl. a sharp catch that NOTES.md's "What Phase 3 must do" fallback — align on (organism, agent) rather than experiment_id — is itself ambiguous for 10 of 26 gold records, since harrison2009, miller2011, martinmontalvo2013, strong2016 acarbose and calubag2025 each have two records sharing that pair; the entry establishes the fact four paragraphs earlier but the recommendation does not carry it forward, so a Phase 3 reader acting on the bolded sentence gets a matcher that collapses 10 records into 5 ambiguous pairs. VERDICT: APPROVE.

Iteration 6 totals: 4 REQUIRED (6a) + 0 (6b). Wiring half approved three times of four. All four remaining items are in the extraction core, and 6b independently confirmed that fixing 6a's item 1 at the model layer is the right place — no CLI change needed. Loop at iteration 6 of the extended cap of 7.

iter 6 fixes + CONTRACT SWEEP | human structured this round as fixes plus a systematic sweep rather than waiting for reviewers to surface decorative tests one at a time. PART 1, the four 6a findings: `_NonFiniteNumber` became `_RefusedNumber(token, reason)` and `parse_payload` gained a third hook, `parse_int=_bounded_int`, which asks int() and converts whatever it refuses rather than restating the 4300-digit cap — orchestrator confirmed a 5000-digit literal now raises ExtractError instead of escaping as a bare ValueError. No CLI change, as both 6a and 6b required. Excerpt tests rebuilt on ~670-character payloads with a marker beside the fault and a different marker at the head, so `position=None` fails both assertions at both call sites. Cost cascade pinned by value with a dated hand-maintained price table in the test (no network, no dependency), failing in both directions of "equal". `_inherited_description` covered both ways. 7/7 mutations red, each caught by exactly one test. PART 2, the sweep: 71 function-level entries in extract/, 62 given a deliberate contract-inverting mutation, 107 mutations total, each applied in a scratch copy with sources restored from a snapshot in a finally between runs. 16 survived; the implementer identified 2 of its OWN mutations as faulty (a `pass` before a `raise` is a no-op; `except UnicodeEncodeError` -> `except UnicodeError` is a widening, not an inversion) and re-ran them properly — both contracts are in fact pinned. That left 14 genuine survivors: repair_json returning a body it cannot fix unchanged, schema_document's per-process cache, extract_record's verbatim-against-the-prompt guarantee, load_schema's document type, build_extraction_schema's empty-items guard, check_quotes_verbatim treating case/punctuation/unicode differences as real, _closest_window, _ellipsis, _resolve_composition's application order, _is_code_assigned's experiment-level-only pruning, _error_path's rendering, build_parser's --limit default vs its own help text, plus two unstated ones. 12 fixed (contract stated in a docstring, PLAN.md or CLAUDE.md) with 32 new tests, all 12 mutation-verified red. 2 LISTED NOT FIXED per the human's instruction to show the true shape of the gap: `Classification`'s immutability (nothing claims it; unfreezing changes no observable behaviour) and `extraction_schema`'s cache (its docstring, unlike schema_document's, makes no caching claim) — both re-verified still green after the fixes, confirming they were not quietly fixed to shrink the table. 9 entries could not be meaningfully mutated and are named as holes in the sweep rather than omitted: two Protocol stubs with `...` bodies, five method-less error classes, RunSummary, and the __main__ shim. 79 of 93 sweep mutations turned red — every honesty invariant, both loud-failure paths, the identity generator, the transliteration table, the gold-set write guard. PART 3: the eval-alignment fallback withdrawn. ORCHESTRATOR VERIFIED THE COUNT AND 6b UNDERSTATED IT — the ambiguity is 12 of 26 records across 6 groups, not 10 across 5. The missed group is eisenberg2009, whose yeast and fly records both read `organism: "other"` with agent `spermidine`; `species` is the only field separating them and `(organism, agent)` does not read it. Entry now states the fallback does not survive the paper's own facts and is withdrawn, gives the verified count and the eisenberg2009 reason, notes that collapsing is the worse half of the id problem because it reports the collapse as agreement, and reserves the alignment key to the human. No replacement proposed, as instructed. Orchestrator verified: ruff clean, 960 -> 999 tests green, gold untouched, nothing staged. Implementer flagged out of scope: `parse_payload`'s decode excerpt takes `last.pos`, an offset into the last candidate tried, but builds the error with the RAW body — reproduced with a fenced payload where repair strips 12 characters, putting the fault at 278 in the candidate and 272 in the raw body; usually still inside the 200-char radius but a long prose preamble shifts it arbitrarily. Also: no test runs `python -m extract`, so the entry point the whole CLI contract is written for is unexercised.

iter 7a | REQUIRED: 2 | extraction core, fresh context, tasked explicitly as the check on the self-administered sweep. Reviewer ran 41 contract-inverting mutations, 38 kills. AUDIT OF THE SWEEP: all 11 of the 12 claimed fixes that live in this scope were independently broken and every one died; the four 6a fixes died across five mutations; the 2 listed-not-fixed rows genuinely survive and are genuinely unstated (Classification's docstring makes no immutability claim and nothing hashes it; extraction_schema's docstring, unlike schema_document's, makes no caching claim) — correctly filed, not misfiled to shrink the table; and 3 of the 9 declared holes were mutated anyway and ALL THREE were killed, so the sweep under-claimed rather than over-claimed. Reviewer judged 14 roughly right for what a function-at-a-time method can see, and both of its own findings fall in categories the method structurally cannot reach. (1) extract/model.py:254-261 — the implementer self-disclosed this and the reviewer escalated it to REQUIRED with a sharper case. `repair_json` only ever shrinks, so when both candidates fail, `last.pos` is in repaired coordinates while `payload=body` is the raw string. On a prose-wrapped response — exactly what repair_json exists for — the window misses the fault entirely: a 448-char preamble puts the true fault at ~558 and last.pos at ~110, and the emitted excerpt is 310 characters of the preamble with the fault marker absent. Required rather than cosmetic because parse_payload's own docstring names this branch and is false for it; the sibling `_RefusedNumber` branch fifteen lines above passes `payload=candidate` and gets it right, so it is an inconsistency inside one function; and the test written THIS ROUND in response to 6a's excerpt finding cannot reach it, because `long_payload()` has no fence and no prose so repair returns it unchanged and `candidates` has length 1. That is NOTES.md's own second fixture rule failing one round after it was written down. (2) extract/model.py:225-243 — a deeply nested payload leaves the module as RecursionError, which is a RuntimeError and therefore neither JSONDecodeError nor _RefusedNumber; it passes both handlers, the retry, and the per-paper `except ExtractError`, ending a batch on a raw traceback. Orchestrator reproduced: depth 200 parses, depth 1000 and 2000 escape. Same defect `_bounded_int` was added this round to close, through a different door, and it falsifies the absolute claim in parse_payload's docstring that nothing from a model payload may leave as anything but a ModelResponseError. ~2 KB of `[` reaches the threshold, well inside MAX_TOKENS 16000. Reviewer verified the downstream analogues are NOT affected — a deeply nested payload that parses is rejected by validate_record before _iter_claims recursion matters, checked at depth 400. 4 SUGGESTED. Reviewer found NO decorative test in this scope. VERDICT: CHANGES REQUIRED.

iter 7b | REQUIRED: 1 | interface and wiring, fresh context, also tasked as the check on the sweep for its scope. Reviewer ran 43 mutations, 39 caught. RULED on the open question: argparse `help=` strings ARE a stated contract — "--help is documentation the program emits at runtime, the only specification of --limit's default an operator ever reads; a help string that interpolates DEFAULT_LIMIT while default= says something else is a program that lies about itself, and worse than a lying docstring because a user acts on it." Independently verified the build_parser fix across 5 mutations, all caught. Re-confirmed the 5b --limit repair holds. Re-verified every previously cleared item by mutation — 12 of 12 hold, incl. the gold guard across 4 separate weakenings, both typed re-raises, the finally cleanup, and the export-parity test. THE ONE REQUIRED: tests/test_extract_cli.py:218 — `_papers` guards `if not paper.abstract or not paper.abstract.strip():` but the fixture only ever supplies `abstract=None`, so the `.strip()` arm has no test. Orchestrator reproduced: deleting the arm leaves all 999 green. Not dead code — orchestrator confirmed ingest/models.py:133 stores `abstract=abstract` verbatim with no normalisation, so a PubMed `<AbstractText> </AbstractText>` reaches the database as "   ". If the arm regresses, that paper is no longer counted no_abstract; it reaches `_process`, `abstract or ""` yields "   ", classify raises, and the paper is counted FAILED with the run exiting 1 — a data condition PLAN.md treats as a warning at exit 0 silently becomes a pipeline failure. Required because the contract is written down twice (`_papers`' docstring "Skipped papers — no abstract"; `_process`' docstring "has already established that the abstract is non-empty"), which is the sweep's own stated criterion for its twelve fixes, and because it is exactly the failure mode NOTES.md's own second rule names — the rule keys on whitespace, the fixture has None. `_papers` WAS swept and this belongs on the fixed list; it is on neither that list nor the deliberately-unfixed one. 8 SUGGESTED, incl. three more unpinned survivors the sweep missed (`order_by(dedup_key)` deletable — SQLite hides it but PostgreSQL row order is unspecified, so which twenty papers --limit picks would become nondeterministic; `_report`'s already-extracted branch; `record_path`'s readable-slug half), plus two NOTES.md arithmetic ambiguities and a possible off-by-one in the nine-holes list (ModelResponseError also declares no methods and is neither in the named four nor called out). NOTES.md fact-checked independently and confirmed correct on every checkable claim, including 12/26 across 6 groups and the eisenberg2009 reason. VERDICT: CHANGES REQUIRED.

Iteration 7 totals: 3 REQUIRED (2 in 7a, 1 in 7b). Both passes independently audited the self-administered sweep and found it honest — 11 of 12 in-scope fixes re-broken and killed, the 2 listed-not-fixed rows genuinely unstated, and 3 of the 9 declared holes mutated anyway and all killed, meaning the sweep under-claimed rather than over-claimed. All three new findings fall in categories a function-at-a-time sweep structurally cannot see: a contract spanning two functions (7a-1), an exception type nobody thought to raise (7a-2), and a two-arm guard whose second arm the fixture never reaches (7b-1). LOOP CAP OF 7 NOW REACHED with REQUIRED items open. Per the human's instruction, stopping and reporting rather than extending — that call is theirs.

LOOP CAP EXTENDED 7 -> 8 AND EXIT CONDITION CHANGED, Phase 2 only, by the human on 2026-08-12. Recorded here rather than by editing CLAUDE.md, which keeps its cap of 5 and its zero-REQUIRED bar for every other phase. NEW EXIT CONDITION for this phase: zero REQUIRED is no longer the bar. The bar is that **no open finding contradicts a stated invariant**, where a stated invariant is one written in PLAN.md, CLAUDE.md, a docstring, or the argparse help text. Findings that do not contradict a stated invariant are recorded as known items in the PR description and NOTES.md rather than fixed. Rationale, from the shape of iterations 3-7: each round has found real but progressively narrower defects, and the last three all landed in categories a function-at-a-time contract sweep structurally cannot reach — a contract spanning two functions, an exception type nobody thought to raise, a two-arm guard whose second arm no fixture exercises. The rate has not broken, so a bar of "zero findings" is not a bar the loop can be expected to hit; a bar of "nothing contradicts a written promise" is one it can, and it is the property that actually matters for a phase whose output feeds the Phase 3 eval.

iter 7 fixes | 5 items | (1) `except RecursionError` arm added to parse_payload's decode loop, raising ModelResponseError.from_payload with position=None — the decoder reports no offset and inventing one would be a guess; the fault is the payload's SHAPE, not a place in it. Not retried, matching the _RefusedNumber arm's reasoning: repair_json only removes text around the object, so nesting depth is identical in both candidates and a retry is a second identical failure. Orchestrator confirmed depth 200 parses and 1000/2000 now raise ExtractError. Docstring amended per the human's instruction to enumerate rather than claim completeness — it now lists the four enumerated doors and states plainly that "the list is the doors that have been found. Two of the four were found separately, after review, each having survived the pass that closed the one before it — so an unqualified claim that nothing else can escape is not one this docstring can stand behind." The implementer also softened `_bounded_int`'s parallel absolute claim, reasoning that leaving one docstring absolute and one honest would re-create the problem one function away — a scope call it flagged rather than took silently. (2) `last_candidate` tracked beside `last` and passed as `payload=`, matching the sibling branch exactly. (3)+(4) test-only; the production code was already correct and untested. The abstract test now covers both arms and adds `summary.failed` to the asserted tuple so the exit-status consequence is pinned, not just the count — deleting the strip arm gives (1,1,1) != (2,0,0), which is exactly the production consequence: no_abstract 2->1, considered 0->1, failed 0->1, run exits 1. The new ordering test inserts 19587684/81/83/80, asserts `inserted != sorted(inserted)` as a stated premise, and asserts the QUERY's output equals sorted — avoiding the trap 7b identified, where the old test asserted a property of its own fixture's constants. The misleading "Asserted, not assumed" comment on the limit test was corrected to say plainly what it checks. Both new tests confirmed to fail under their mutations while the limit test passes, so neither duplicates it. (5) NOTES.md arithmetic reconciled and now internally consistent: 93 sweep (79 red / 14 green) + 14 follow-ups (12 red / 2 green) = 107 total, 91 red, 16 green, with the two self-inflicted faulty mutations explicitly located among the follow-ups rather than the 93. IMPLEMENTER CORRECTLY REJECTED 7b's off-by-one suspicion WITH EVIDENCE rather than complying: `ModelResponseError` DOES declare a method of its own, `from_payload` at extract/errors.py:102 (orchestrator confirmed), so it was never a candidate for the method-less group and is an ordinary swept entry — verified covered rather than assumed, since making from_payload ignore its position turns 2 tests red and dropping the excerpt turns 14 red. The hole count stays at NINE, and the nine still sum to 71 - 62. 6 mutation arms, all red. Orchestrator verified: ruff clean, 999 -> 1005 tests green, gold untouched, nothing staged, HEAD still 185eae7. Gold guard rail byte-identical. Implementer flagged out of scope WITH the human's stated-invariant classification applied: ingest/{biorxiv,pubmed}.py have item 1's defect (RecursionError escapes `except ValueError` on response.json()) — does NOT contradict a stated invariant, Phase 1 code; parse_payload's `last is None` branch is unreachable dead defensive code — does NOT contradict; `extract/__main__.py` still untested — does NOT contradict, a coverage hole rather than a false claim. It also probed depths 986-999 through extract_record and confirmed every one leaves as an ExtractError.

iter 8a | REQUIRED: 1, KNOWN ITEMS: 6 | extraction core, fresh context, first pass run under the human's new exit condition (a finding is REQUIRED only if it contradicts an invariant stated in PLAN.md, CLAUDE.md, a docstring or the argparse help text). Reviewer ran 69 deliberate mutations plus ~30 adversarial payload fuzz probes; 59 caught, 10 survived, 5 of those provably equivalent mutants. THE REQUIRED: extract/model.py:369 `_create` catches `anthropic.APIError`, but the SDK's exception ROOT is `anthropic.AnthropicError` — orchestrator confirmed APIError's MRO parent is AnthropicError and that `AnthropicError`, `RetryableError` and `WorkloadIdentityError` are all exported and all outside the caught subtree. Verified escaping `call_structured` and `classify` as themselves, isinstance(ExtractError) False. Contradicts two docstrings in the file under review: `_create`'s "converting ANY SDK failure into a ModelCallError" and `call_structured`'s "NEVER lets an SDK exception escape — a caller processing a batch must be able to handle one paper's failure by catching one type". Not hypothetical: the SDK's own _base_client documents that middleware exceptions propagate as-is except those opting into the retry policy by type, and RetryableError is one — so a RetryableError surviving the SDK's retry budget reaches `_create` as itself, giving a raw traceback past the retry, past the per-paper handler, stranding every later paper. Nothing caught it because all three SDK-error tests build their input from APIError subclasses — the fixture cannot reach the failure, which is the second rule NOTES.md's Test honesty section ends on. Fix is one identifier; reviewer applied it in a scratch copy, 1005/1005 pass and all three convert with status=None since from_api_error already handles a missing status_code. THE SIX KNOWN ITEMS (real, contradicting nothing written down, destined for the PR description): the RecursionError arm's in-comment reason for not retrying is factually false (repair_json's outermost-brace slice DOES remove nesting — `"["*2000 + '{"a":1}' + "]"*2000` repairs to depth 1 and would parse), and the two sibling comments are wrong the same way, though the behaviour is conservative refusal in the safe direction; `_FENCE_RE` is dead against the whole suite (deleting the fence rule leaves 1005 green, since the outer-brace slice already recovers every fenced case tested); this round's coordinate-space fix has three unpinned siblings, all correct but unguarded, which is the bug class fixed this round one branch over; `load_schema` converts two read failures but lets IsADirectoryError/PermissionError/UnicodeDecodeError escape; `_ID_PATTERN` duplicates the schema file's identifier regex with nothing pinning them equal; and `experiments.get("description", "")` is a live default. ON THE FIFTH DOOR: reviewer looked hard and found none in parse_payload — enumerated everything json.loads can raise under the hooks, established TypeError and UnicodeDecodeError are unreachable because response_text always returns str, measured repair_json cannot raise (2 MB body, 0.7 ms, no backtracking blowup), fuzzed ~30 adversarial bodies and 7 adversarial records, and every failure came out as an ExtractError. The door it did find is on the SDK side, not the payload side. Confirmed this round's two fixes are correct and properly pinned, and the amended docstring honest. Reviewer did not dispute any of the three carried classifications and independently re-derived the `last is None` one. 3 SUGGESTED, incl. a precise catch that the amended docstring's "Four ways out of json.loads" miscounts by one and the same sentence says why — the bare ValueError from an over-long integer never leaves json.loads, being caught in `_bounded_int` and re-raised as a `_RefusedNumber`; three exception types can leave, four origins feed them. VERDICT: CHANGES REQUIRED.

iter 8b | REQUIRED: 0 — APPROVE | KNOWN ITEMS: 5 | interface and wiring, fresh context, run under the new exit condition. 39 mutations in a scratch copy, real tree never modified. Both new tests confirmed load-bearing: deleting the `.strip()` arm produces exactly the claimed `assert (1,1,1) == (2,0,0)` with failed 0->1 and the run exiting 1 ("accurate in every digit"), and dropping the order_by fails the ordering test on the QUERY's output, with the premise assertion separate from the claim — the trap 7b identified is avoided. Every previously cleared item re-confirmed by inversion: gold guard and chokepoint status, _check_writable, _write_record atomicity/finally/both typed re-raises, both OSError handlers, the rewritten --limit test, the export-parity test, and build_parser's help/default agreement. Reviewer disclosed that its own first attempt at the allow_nan mutation hit the docstring mention rather than the code and falsely survived — re-run correctly, it is red. VERIFIED THE IMPLEMENTER'S REBUTTAL OF ITS OWN PREDECESSOR AND CONFIRMED IT: ModelResponseError does declare from_payload at errors.py:102, so it is a function-level entry rather than a method-less class; the offered evidence reproduces exactly (position ignored -> 2 red, excerpt dropped -> 14 red). Hole count stays at NINE. Also independently recounted the 71-entry universe by NOTES' own stated rule and got 71 exactly (1+1+3+13+8+14+14+17), with 71-9=62 mutated, and confirmed the corrected arithmetic reconciles in every figure. FIVE KNOWN ITEMS, three of them documentation defects in NOTES.md that should reach the PR description verbatim since NOTES.md is Phase 3 input: (a) the Test honesty section's lead sentence says "Three tests in this repo have now been found decorative" and is contradicted four paragraphs later IN THE SAME DATED SECTION by the heading "Cases 4 to 7" — one same-day section disagreeing with itself, in the document PLAN.md Phase 0 designates as the README's Design Decisions source, and "three" is the quotable sentence; (b) "Five are in the reporting layer" names four things for five items, and mapping the twelve findings onto the five stated buckets leaves repair_json in none — the taxonomy sums to 12 only via a one-item overcount; (c) `data/extracted/` is undocumented outside code, appearing only in .gitignore and cli.py while PLAN.md's architecture block enumerates data/gold/ and data/drafts/ and NOTES.md never records the choice, so Phase 3 will read both and not find the directory its eval inputs live in. Plus: `_check_writable`'s probe mechanism is unpinned (replacing the whole mkstemp probe with os.access leaves 1005 green, though the docstring explicitly refuses that approximation and names the full-disk case os.access cannot detect), and the --limit help text names only one of the two skips that do not consume the limit. VERDICT: APPROVE.

Iteration 8 totals under the NEW exit condition: 1 REQUIRED (8a) + 0 (8b), and 11 KNOWN ITEMS. The single REQUIRED contradicts two docstrings in extract/model.py and is a one-identifier fix. THE PHASE DOES NOT YET MEET ITS OWN EXIT BAR — one open finding contradicts a stated invariant. Not fixed: the human authorised iteration 8's five items and a review, not a further fix round. Reported with the drafted PR description for their call.

iter 8 fixes | 3 items | (1) extract/model.py:381 `except anthropic.APIError` -> `except anthropic.AnthropicError`, one identifier. Implementer verified the hierarchy itself against the installed 0.121.0 rather than taking 8a on trust: of 19 exported *Error classes exactly three are AnthropicError but not APIError, all three construct from a single message string and none carries an int status_code, so from_api_error reports status=None. Orchestrator independently confirmed the same three. Docstring amended — it now states the caught type is the SDK's exception ROOT, names the three that were escaping, explains why RetryableError is the one with teeth, and records status=None as the expected reporting. NEW TEST derives its universe from the installed anthropic package (every exported exception that is an AnthropicError and not an APIError) rather than a hand-written list, on the reasoning that a hand-written list of SDK error types can be missing exactly the type the except clause is missing — the same failure one level up; a companion test asserts the remainder is non-empty, because a parametrization over nothing is a green test that ran nothing. Mutation: reverting turns all 3 new cases red and leaves the three older SDK tests GREEN — the three green ones are the finding, since their fixtures are all APIError subclasses and no assertion in them could reach the failure however written. (2) Test honesty arithmetic corrected BEFORE extending, per the human's ordering. Implementer enumerated every logged case from LOOP_LOG.md and derived SEVEN across four rounds (3a, 4a, 5b, and four from 6a), cross-checked two ways: 5b's own "the third decorative test" pins cases 1-3, and the iter-5-fixes entry shows the section was written recording three — so "three" was TRUE the day it was written and was never re-derived after 6a added four. The "Cases 4 to 7" heading was right; the lead sentence was the stale one, and the correction is recorded as a correction rather than silently re-worded. The seven are split honestly rather than lumped: four are tests that exist and cannot go red, three are coverage absent outright, and within the first four, two assert the wrong thing while two have fine assertions over unreachable fixtures. Bucket taxonomy: 8b's mapping re-derived and confirmed — 2+2+2+1+4 = 11, "Five" corrected to "Four", every member now named inline so the count is checkable, and the twelfth (repair_json) stated plainly as fitting no bucket, with the text refusing to widen one to absorb it ("an accounting that balances by widening a category until everything fits describes nothing"). All 17 other figures in the section re-verified against the log. (3) data/extracted/ documented in PLAN.md's architecture block (exactly two lines, orchestrator confirmed the diff touches nothing else — no phase scope, deliverable, Done-when, checklist item or working agreement) and in a new dated NOTES.md section covering what it holds, that `python -m extract` is the only writer, why it is gitignored, why resolve_out_root refuses data/gold/, and that Phase 3 reads it — ending with the JSON-vs-Postgres choice stated as an open question reserved to the human, with the case each way and no decision taken. Orchestrator verified: ruff clean, 1005 -> 1009 tests, gold untouched, nothing staged. Implementer flagged one NEW out-of-scope coverage hole with the classification applied: `classify` has no SDK-error test at all — every SDK-error test drives call_structured directly, so the shared code path is pinned but the classify entry point is not. Does NOT contradict a stated invariant.

iter 9a | REQUIRED: 1 (documentation-only), KNOWN ITEMS: 8 | extraction core, fresh context. 71 mutations, 61 caught, 10 survivors of which 4 provably equivalent and 5 are listed known items. Confirmed this round's fix: reverting AnthropicError -> APIError turns exactly the three new cases red and leaves 1006 passing INCLUDING the three older SDK tests — the three staying green is itself the finding. Confirmed the premise test does real work: stubbing the derived set to [] makes the premise test fail with its own message while the parametrized test degenerates to "SKIPPED got empty parameter set", exactly the silent pass it was written to prevent. THE REQUIRED, and it is a sharp one: the docstring amended THIS ROUND specifically to be honest introduced a false checkable fact. extract/model.py:375-377 says "None of the three carries a status_code, so from_api_error reports status=None and names the class". `WorkloadIdentityError` DOES — it declares `status_code: Optional[int]`, and the SDK's own raise site at anthropic/lib/credentials/_workload.py:96 sets it from the response. Orchestrator confirmed independently: the signature accepts status_code, and from_api_error on a 401 instance reports status 401, not None, with the class unnamed. The same false rationale is copied into tests/test_model.py:542-544, where `assert error.status is None` passes only because the fixture builds `error_type("boom")` with no status — a property of the fixture, not of the type, and WorkloadIdentityError is precisely the type that models an HTTP response. REQUIRED because a docstring is a stated invariant under this phase's bar and an amended docstring that is inaccurate is a finding. Reviewer was scrupulous about severity: the BEHAVIOUR is fine either way (reporting HTTP 401 is arguably better than naming the class), and the path is unreachable as wired, because make_client() always passes an explicit api_key and the SDK only runs the credentials chain when credentials, api_key and auth_token are all None. Fix is one sentence plus the test comment, ideally asserting per-type rather than a blanket status is None. 8 KNOWN ITEMS, incl. two that qualify the round's own claims honestly: "the SDK's exception root" is the root of the EXPORTED hierarchy, not the package (MissingStreamClassError and ToolError sit outside AnthropicError, neither reachable from a non-streaming create), and non-SDK exceptions still escape `_create` — an unexpected kwarg or an unserialisable body come out as bare TypeError, unreachable today because all five request keys are valid in 0.121.0 and the schema is derived from a JSON file, so call_structured's guarantee holds because the request dict is fixed rather than because the catch is total. Also: the derived-universe test derives from the same premise the code relies on, so a future SDK exception OUTSIDE AnthropicError is invisible to it; MAX_TOKENS and both system prompts are wholly unpinned (setting MAX_TOKENS=100 or stubbing either SYSTEM_PROMPT leaves 1009 green, so the not_reported / no_effect / median-vs-max rules PLAN.md names as the hardest fields have no regression guard and only Phase 3's evals would notice a gutted prompt); and `_claim_value`'s `value.strip()` arm is unpinned though load-bearing. Reviewer judged 8a's payload-side search SOUND and found no fifth door, adding one non-exception observation: duplicate keys in a model object are silently last-wins since object_pairs_hook is unset. Disputed no carried classification. VERDICT: CHANGES REQUIRED.

iter 9b | REQUIRED: 0 — APPROVE | KNOWN ITEMS: 7 + carried | interface and wiring, fresh context. 60 mutations, 47 killed; reviewer disclosed 2 of its own 13 survivors as faulty mutants (a whitespace no-op rather than a contract inversion, and a mkdir mutation that appended a statement instead of removing parents=True) rather than reporting them as findings. INDEPENDENTLY VERIFIED ALL FOUR OF THIS ROUND'S CHANGES. The seven-case count re-derived from LOOP_LOG.md and confirmed, including that 6a's `_inherited_description` finding is two independently deletable sites (schema.py:308 is the `source is not node` skip inside _flatten, 367 is _inherited_description itself), giving 4+5+6+7 from 6a; also noted approvingly that the lead sentence now says "cases" rather than "tests", which is the honest word given three of the seven are missing tests rather than bad ones. Bucket arithmetic re-derived: 4+2+2+2+1 = 11, +repair_json = 12, matching the bullet list's 12 entries. Whole section reconciles. Nine-holes membership independently confirmed against errors.py — exactly five error classes declare no methods, ConfigurationError is the mutated representative, four are holes, ModelCallError and ModelResponseError correctly excluded. Iteration-8 subsection fact-checked against the installed SDK and found accurate. data/extracted/ documentation: every NOTES claim checked against the code, including confirming directly that RawPaper has NO "extracted" column, so "the existence of the file is the only marker" is literally true, and that `python -m extract` really is the only writer. The JSON-vs-Postgres section confirmed to genuinely leave the question open — it cites PLAN.md's stack line and Phase 4's endpoint verbatim and closes with "The decision is the human's." Every item on the re-verify list re-killed: gold guard 4/4, _check_writable 3/3, _write_record 5/5, both OSError handlers 2/2, --limit, export parity 5/5, help/default agreement 4/4, both abstract arms, order_by. 7 KNOWN ITEMS. One is a classification the reviewer explicitly flagged for the human to overrule: PLAN.md:22's "one record per paper" is ambiguous against PLAN.md's own use of "record" (line 19 says "experiment records" and Phase 2 says multi-organism papers yield multiple), so read that way it is false since eisenberg2009 has three — filed as KNOWN rather than REQUIRED only because the schema's own top-level title is literally "lifespan-extract paper record", under which the line is accurate, and NOTES' parallel sentence is unambiguous. One word fixes it. Also: both parser default VALUES are unpinned while only their agreement with --help is (the CLASSIFIER_MODEL shape from 6a, one file over), and `extracted_from=ABSTRACT` can be flipped to "full_text" with the suite green — no CLI test asserts a written record's provenance field — as can swapping classify's two arguments. Sharper instance of the carried __main__ gap: `sys.exit(main())` reduces to `main()` with the suite green, so `python -m extract` would exit 0 on a batch with failures. Correctly classified as coverage rather than contradiction — the code does what the docstring says; nothing pins it. VERDICT: APPROVE.

Iteration 9 totals: 1 REQUIRED (9a) + 0 (9b). The single REQUIRED is documentation-only and self-inflicted by this round: the docstring amended specifically to be honest introduced a false checkable fact about WorkloadIdentityError's status_code. Behaviour is unaffected and the path is unreachable as wired. THE PHASE STILL DOES NOT MEET ITS OWN EXIT BAR — one open finding contradicts a stated invariant. Not fixed: the human authorised iteration 9's three items and a review, not a further fix round.

iter 9 fixes | FINAL PASS, no reviewer after it, by the human's ruling (route 3: fix and stop reviewing). (1) The false docstring sentence corrected and, per the human's "say what the catch covers, not what would be tidy", the replacement states the reporting is PER-TYPE: AnthropicError and RetryableError declare no status_code so the wrap names the class, while WorkloadIdentityError declares one the SDK fills from the token-exchange response and is reported as that HTTP status. The count "three" is now framed as a dated observation ("Under anthropic 0.121.0") pointing at the test that derives it, rather than a standing fact, and the two 9a qualifications are folded in — the caught type is the root of the EXPORTED hierarchy, with MissingStreamClassError (SSE only) and ToolError (tool runner) named as sitting outside it and unreachable from a non-streaming create. The test comment was corrected and the blanket `assert error.status is None` replaced by a per-case `assert error.status == expected_status`, parametrized over (exc, expected_status) pairs including a WorkloadIdentityError carrying 401, with the expected status written BESIDE the instance rather than read back off the exception so it cannot pass by mirroring the implementation. The premise test gained a line asserting at least one case carries a status, so that branch cannot silently go vacuous on an SDK change. Mutation: forcing status = None turns the 401 case red, where the old blanket assertion would have stayed green. (2) PLAN.md "one record per paper" -> "one file per paper", one word, as the human ruled — 9b had filed it as a known item and was overruled. The alignment defect was fixed by moving the description onto the continuation line, since `data/extracted/` is exactly 15 characters and no one-space spelling aligns with the siblings. Orchestrator confirmed the diff touches nothing else. (3) `extracted_from` now pinned on a record actually read back off disk, plus assertions on what the two model calls were shown. Both mutations red: flipping ABSTRACT to "full_text", and swapping classify's two arguments. The `claims` walker moved to conftest.py so both test files share one shape-based definition. (4) MAX_TOKENS and both system prompts deliberately LEFT unpinned per the human's ruling that evals are the right instrument — a unit test asserting prompt text pins the wording, not the behaviour. Recorded in the PR known-items list with that reason. THE FINAL PASS FOUND ONE MORE, and classified it honestly rather than working around it: extract/errors.py:75-78, `from_api_error`'s OWN docstring, enumerated "connection failures and timeouts" as the None cases — incomplete in exactly the way the model.py sentence was wrong, since a bare AnthropicError and a RetryableError also yield None and are neither. Implementer classified it YES against the exit condition and handed it back rather than widening scope on a pass with no reviewer. Orchestrator closed it directly: fixing the caller's description while leaving the function's own false is the "one absolute and one honest re-creates the problem one function away" failure the human accepted reasoning about two rounds ago, and it is entailed by item 1's instruction. The docstring now states the rule (integer status_code -> HTTP status, otherwise None) and names what None actually covers. Final state: ruff clean, 1009 -> 1011 tests, gold untouched, nothing staged, HEAD still 185eae7.

PHASE 2 REVIEW LOOP CLOSED. Nine iterations, eighteen reviewer passes, run as two scoped passes after a single full-diff dispatch failed twice at the tool-use layer. Cap extended 5 -> 7 -> 8 and the exit condition replaced, both recorded above. Final position under the phase's bar: no open finding contradicts an invariant stated in PLAN.md, CLAUDE.md, a docstring, or the argparse help text. Known items carried into the PR description rather than fixed. Still unverified for want of ANTHROPIC_API_KEY and DATABASE_URL: the live API round trip, whether the derived request schema is accepted by the structured-output endpoint, prompt quality on real papers, and therefore PLAN.md's Phase 2 "Done when" — 20 unseen papers end to end.

HANDOFF 2026-08-12 — Phase 2 incomplete, review loop not closed. Branch `phase-2-extraction`, 19 files staged (+3001/-3), nothing committed, `data/gold/` untouched; `.venv/bin/ruff check .` clean and `.venv/bin/pytest` green at 626. Iteration 1 built `extract/{errors,schema,model,classify,extract,cli}.py` with a Haiku-4.5 classifier gate and an Opus-5 structured-output extractor, and the fresh-context reviewer returned four REQUIRED items, all four addressed in iteration 2: (1) `source_quote` was never checked against the source text — a fabricated quote produced a schema-valid record — now `extract/schema.py::check_quotes_verbatim` does exact-then-whitespace-collapsed matching mirroring `scripts/check_gold.py`, called from `extract_record`, with `collapse_whitespace` duplicated rather than imported (to avoid depending on the gold tooling) and the two pinned together by a parity test; (2) the `anthropic>=0.69` floor predated `output_config`, which first shipped in 0.77.0, so the floor is now `>=0.77` with the reason in the comment; (3) `anthropic.APIError` escaped the per-paper handler and aborted the whole batch, now wrapped as `ModelCallError` at the `extract/model.py` boundary so failures are counted and logged and the run continues; (4) the generated `experiment_id` cannot reproduce 15 of 26 gold ids (human disambiguates semantically `-male`/`-low-dose`, generator numerically `-2`; human hand-normalises agent names, generator does not) — the generator was deliberately left following the schema's documented convention because choosing between the two is the human's call, and the divergence is instead pinned by a test parametrized over the real gold files plus a dated NOTES.md entry. I independently mutation-tested item 1 as orchestrator: disabling the `check_quotes_verbatim` call turns exactly three tests red and restoring it returns the suite to green, so that check is load-bearing rather than decorative. **The iteration-2 review has not run** — the reviewer dispatch was rejected at the tool-use layer for infrastructure reasons, not by any policy or finding, so the loop is stopped after one completed review round with four fixes unreviewed. Phase 2 is NOT done: per CLAUDE.md the definition of done requires a fresh-context review returning zero REQUIRED items, and no PR should be opened until that runs. Resume by re-dispatching the reviewer against `git diff --cached main` (a saved copy is at `scratchpad/phase2-iter2.diff`, 3148 lines) and continuing the loop from iteration 2. Still unverified for want of an `ANTHROPIC_API_KEY` and a `DATABASE_URL`: the live API round trip, whether the derived request schema is accepted by the structured-output endpoint, prompt quality on real papers, and therefore PLAN.md's Phase 2 "Done when" (20 unseen papers end-to-end). Open questions for the human: the `experiment_id` convention above; whether extracted records belong in `data/extracted/<schema_version>/` as JSON or in Postgres; and the classifier's deliberate bias toward answering true on ambiguous text.

iter A+D | REQUIRED: 4 (3 in pass a, 1 in pass b) | The first live run returned HTTP 400 — 24 union-typed parameters against an endpoint limit of 16 — which nine review iterations could not have caught, because every test stubs messages.create and validates the schema against jsonschema rather than the service. Human chose A+D, rejecting B (rests on the same class of unverified live-endpoint assumption that just failed), C (loses type checking on the four percent fields PLAN.md names as hardest, and makes Phase 3's numeric tolerance run on parsed strings) and E (mean_change_pct is the median-vs-max discriminator). Implemented: A as an override in build_extraction_schema only, NOT a v0.5.0 schema change, with a drift test that fails if any of the four quotes is null anywhere in data/gold/; D as a 10/10 split, call 1 establishing the experiment list and order, call 2 shown the same paper prompt plus a numbered echo and returning one outcome per listed experiment carrying a required integer experiment_index. 1012 -> 1077 tests. Orchestrator verified: identity 10 unions, outcome 10, partition disjoint; neutralising the A override -> 3 red; disabling the merge one-to-one check -> 6 red. PASS a REQUIRED: (1) `_merge_outcomes`'s docstring says its "already set" guard cannot fire on model output, but `_refuse_unknown` runs on the outcome half only, so an identity response volunteering `mechanism` or `lifespan_effect` reaches it — and the message then misdirects the operator to two constants that are correct, carrying no payload excerpt because it is an ExtractError rather than a ModelResponseError; the paired `_refuse_unknown` docstring also mis-states additionalProperties for what the schema actually declares as unevaluatedProperties, and the two docstrings contradict each other. (2) THE JOIN IS BY INDEX BUT NOTHING DISTINGUISHES IT FROM A POSITIONAL JOIN — orchestrator independently confirmed that `zip(indices, outcomes)` -> `enumerate(outcomes)` leaves all 1077 passing. A reordered response is a legal response the code promises to handle by index; under the mutation every outcome attaches to the wrong experiment and the record still validates, satisfies check_provenance and passes check_quotes_verbatim, because the quotes are all real sentences from the same paper. That is the silent misattribution the whole experiment_index mechanism exists to prevent, and it is the one member of that family with no test. (3) the comment claiming the shared prompt blocks are the old prompt "cut, not reworded" is false — two textual changes survive, and one of them ("from the text above" -> "from the paper text above") is load-bearing for constraint 5. PASS b REQUIRED: conftest's `extraction_responses` docstring claims a property moving between halves "changes no test"; moving `mechanism` turns two red. Pass b filed it strictly under the stated rule and said so. PASS b KNOWN ITEMS, two of which matter more than its REQUIRED: the new "Open questions reserved to the human" index claims "Every decision this log has deliberately left open is listed here" and is missing at least four — free-text field scoring (NOTES:360), the calubag2025 ambiguity-vs-silence question whose own text says it "joins ... the list of things Phase 3 must settle before the metric is written" (NOTES:551), the v0.4.0/v0.5.0 schema-shape candidates (NOTES:280,319,400), and the absent held-out split (NOTES:619) — orchestrator confirmed all four; and PR_DESCRIPTION.md never describes A+D at all, still reads as a single-call extractor, states "Not verified" reasons that a live run has since answered (it was asked and it failed), omits the cost doubling entirely, and carries stale counts (1012 vs 1077, "nine iterations"). Pass b also found the permutation gap's root cause in the fixtures: `experiment_payload()`'s outcome half is byte-identical across every experiment any test builds, so no test can observe which experiment an outcome landed on. Constraint 2 verified by pass b through mutation: introducing a real partial-write path turns 8 red. Constraints 1, 4, 5 met; 3 met for drop/duplicate, NOT for reordering.

iter A+D fixes | REQUIRED: 5 (3 in pass a, 2 in pass b) | The positional-join hole is CLOSED — orchestrator re-ran all three mutations on the final tree: positional 3 red, reversal 4 red, one-place rotation 4 red, where all three previously left 1077 green. `experiment_payload` now derives its outcome half from its identity half; `_refuse_unknown` runs on the identity half too, firing before the second Opus call is paid for (removing it -> 3 red, two on "unexpected extra model call"); the four false docstrings/comments were rewritten; the NOTES index was re-asserted complete at ten after a documented grep, with four borderline hits routed to the new DEBT.md; DEBT.md created with 26 entries and a one-item-one-file rule; PR_DESCRIPTION.md now describes A+D, the cost doubling, and a rewritten "Not verified". 1077 -> 1085 tests. Pass b confirmed the previous round's REQUIRED are properly fixed and that the index's completeness claim now holds as far as it could check — a change from last round, and it said so plainly. THE FIVE NEW REQUIRED: pass a (1) the "cut, not reworded" prompt comment is STILL false after being rewritten to correct exactly that — the single _ABSENCE_RULE bullet was rewritten rather than moved, and `a starting age` has no counterpart in the old prompt at all; orchestrator confirmed zero hits for age_at_start/starting age in f4f0f9d's SYSTEM_PROMPT. Third round running that this comment has carried a false statement, and this round was its correction. (2) the new schema-closure test says "derived from the file, so a wrapper added later is covered without an edit" then hard-asserts len(wrappers) == 9; adding a correctly-formed tenth wrapper fails on the count, not the closure. Pass a also refuted the implementer's claim that the test was not mutation-testable — it ran both mutations. (3) `test_the_well_formed_join_is_the_premise_of_the_five_below` names five; there are eight failure tests below it. Pass b (1,2) `payload_identity` and `experiment_payload` both assert an injectivity the derivation does not have. Pass b measured it: of eleven experiments a test could plausibly build, NINE produce byte-identical outcome halves — only `agent=` and an explicit `direction=` escape. Orchestrator independently confirmed the sex male/female and organism/species-collapse cases. This matters because sex and dose are exactly the axes the gold set uses to separate experiments within one paper (harrison2009, martinmontalvo2013), so a future join test written on the project's own natural axes lands in the collision set and is vacuous in precisely the way this round exists to fix — and the docstring is what a future author reads before choosing an axis. Dormant only because the current fixture varies `agent`. 11 KNOWN ITEMS, notable: the organism/species half of payload_identity is entirely unpinned (replacing the body with `return agent` leaves 1085 green); the join family's load-bearingness rests on one premise test attached to `three_experiments()` specifically; four dead cross-references to the retired numbered known-items list ("known item 8", "known item 13") now pointing at nothing; six DEBT entries carry the collection date 2026-08-13 although LOOP_LOG names the iteration that found them, which DEBT's own stated rule says takes precedence; the PR declares the one-item-one-file rule and then restates D22 and D11 in full in the same paragraph, with the 5-of-221 measurement now living in three files; and D11 is a decision sitting in a file whose first line says "Not decisions". BOTH PASSES RULED ON THE EIGHTH-VS-NINTH COUNT AND BOTH REJECTED RECORDING TWO: the section's own text ("Cases 4, 6 and 7, and three of the twelve above, failed for exactly the reason cases 1 and 3 did") supports TWELVE, and only two of that three are identifiable from the text, so the population is undefined rather than the count disputed. Pass b noted the section already prescribes its own remedy one paragraph earlier — "Every member is now named so the count can be checked instead of trusted".

iter hygiene | REQUIRED: 3 (all pass a; pass b APPROVE) | The fixture is now injective BY CONSTRUCTION: `payload_identity` takes the whole payload and serialises the entire identity half after identity overrides are applied, `mechanism` embeds that string verbatim, and `outcome_payload` refuses any multi-experiment fixture whose outcome halves coincide. Orchestrator verified: sex male/female, the organism/species collapse, and strain all now produce DISTINCT outcome halves; the guard fires on two identical experiments; the positional-join mutation stays red at 3. 1085 -> 1096. The prompt-diff comment was DELETED rather than corrected a fourth time, with the one load-bearing fact ("the paper text above", not "the text above") moved to sit above `_QUOTING_RULE` where it is enforced; the implementer also removed the same false claim from NOTES' own "Two system prompts" paragraph, reasoning that deleting it from the code alone would relocate the defect. Pass b confirmed the deletion lost nothing true, having diffed the split prompts against f4f0f9d itself. The Test honesty count is now a NAMED POPULATION — eleven identified plus one described but never named, stated as a pair rather than a total, with the implementer refusing to guess the unnamed member ("a guess entered on a list like this is the failure the list exists to prevent"). Pass b checked the population against LOOP_LOG and ruled it honest and correctly shaped, and confirmed no number in that section is now un-re-derivable. PASS a REQUIRED, all in conftest and all docstring-vs-mechanism: (1) the new derivation's forward-compatibility claim is FALSE AND INVERTED — it says the derivation reaches "a property a later schema version adds to IDENTITY_PROPERTIES that no argument list written here could know about", but it reads a hand-written payload literal, so orchestrator confirmed that adding a seventh identity property gives KeyError at collection and ZERO TESTS RUN. The same false premise is restated in a test docstring claiming it "fails here" — it does not fail there, the file never collects. (2) `extraction_responses` says moving `mechanism` into the identity half "turns two red"; it is 0 red and 2 collection errors, and this is the THIRD CONSECUTIVE ROUND that one sentence has carried a wrong claim — last round's correction of it was invalidated by this round's own change in the same commit. (3) `_refuse_indistinguishable_outcomes` claims it is checked "for every fixture that builds more than one experiment"; three in-tree fixtures reach the merge through `identity_payload` alone and bypass it. Pass a also measured the `mechanism` blob at 924 characters, not the ~600 reported, and argued it is not cosmetic because it truncates the failure output of exactly the tests it exists to make meaningful. PASS b: REQUIRED empty, VERDICT APPROVE. It verified all twelve date re-attributions against LOOP_LOG line by line and found every one correct, including the implementer's own D3 8a->6a correction on the "first wrote it down" rule; confirmed D6/D24 now point at each other; and agreed with the D11 reclassification. Its highest-value finding is a KNOWN ITEM only because the exit condition routes NOTES inaccuracies there: NOTES:1159 still says "`_refuse_unknown` is on the outcome half only" in the present tense and reproduces the very rationale a previous round refuted, contradicting `_merge_outcomes`' own docstring, which correctly uses the past tense. Also: the PR's "Ten iterations, twenty reviewer passes" is stale — LOOP_LOG supports 22 before this round — which is the same never-re-derived-count failure the Test honesty section was written about, now living one file over; and the PR restates the 5-of-221 measurement in one section 60 lines after refusing to restate D22/D11 in another, two sections of one document applying opposite policies.

iter hygiene fixes | 7 items, done by the orchestrator directly after the implementer agent was terminated mid-task by a session limit (it had made no edits; tree verified intact at 1096 before starting). (1)+(4) `payload_identity` now reads the identity half's VALUES via a new `_identity_values` recursion, through `payload.get(name)` — 924 characters down to 240, and a pytest diff under the positional-join mutation now shows the discriminating field ("agent": "acarbose" vs "rapamycin") without -vv, which was the point: that diff is the output of the tests the derivation exists to make meaningful. Injectivity preserved — all three join mutations re-measured red at 3/4/4, and sex, dose, organism-vs-other+species, strain and sample_size all still produce distinct outcome halves. THE FIRST ATTEMPT AT THE FORWARD-COMPAT FIX WAS INCOMPLETE: `payload_identity` alone was not enough, because `identity_payload` and `outcome_half` subscripted the constants too, so a seventh IDENTITY_PROPERTY still aborted collection. Routed all three sites through a new `_half(experiment, names)` helper using `.get`; verified a seventh property now leaves the suite COLLECTING (947 passed) instead of zero tests run. (2) `extraction_responses`' count dropped, keeping the general claim, with the reason recorded in the docstring — that sentence had carried a wrong number three rounds running and twice the correcting round invalidated its own correction in the same commit. (3) the guard's coverage sentence narrowed to what `outcome_payload` actually sees, noting explicitly that a fixture building only the first call through `identity_payload` never passes through it and does not need to. (5) NOTES:1159 corrected to the past tense mirroring `_merge_outcomes`' docstring, and now states BOTH reasons the old rationale was wrong — a real experiment property belonging to the other half never reaches validate_record at all, and even for keys it does catch, catching them there means the second Opus call is already paid for. Line-wrap seam at 1148 closed. (6) PR: the "Ten iterations, twenty reviewer passes" count REMOVED rather than re-derived, pointing at LOOP_LOG instead, on the rule the prompt-comment deletion established; the four-REQUIRED sentence extended to name the rounds after it; PR §5's 5-of-221 and 121-of-221 figures reduced to a pointer at NOTES' quote-shape table, resolving the two-sections-opposite-policies inconsistency. (7) DEBT D27 added under a new "Process" heading, recording the three-round pattern with its instances and explicitly NOT proposing a fix — noting that both mitigations applied so far worked by removing the claim rather than keeping it true, and that whether anything can catch it mechanically is Phase 3's to decide. ORCHESTRATOR SELF-CORRECTION worth recording: the first version of the corrected forward-compat docstring asserted that a new property "reads as null for both experiments and this assertion catches the sameness" — measured and it does not, because the test supplies the property itself as an override and passes. Both docstrings were rewritten to what was actually measured. That is D27's own pattern occurring inside the fix for D27, caught by measuring rather than by review. ruff clean, 1096 tests (unchanged), gold guard rail 11/26 reproduced with KNOWN_ID_DIVERGENCES untouched, nothing staged.

iter hygiene fixes, review | REQUIRED: 1 (pass a; pass b APPROVE) | Pass a found D27's pattern INSIDE THE FIX FOR D27, for the third time in this repo: the corrected `payload_identity` docstring attributed its null-reading to `_half`'s `.get` when the function has its own at line 224. Pass a measured both independently load-bearing; orchestrator reproduced — reverting either one alone, with a seventh IDENTITY_PROPERTY added, aborts collection (2 errors / 1 error). Corrected to name this function's own `.get` and say both are needed. Pass a confirmed the other two corrected docstrings TRUE by running them: moving `mechanism` OUTCOME->IDENTITY turns 2 red and moving `organism` IDENTITY->OUTCOME turns 110 red, exercising all three mechanisms `extraction_responses` names; and no second-call payload is built anywhere outside `outcome_payload`, so the narrowed coverage sentence holds. 4 KNOWN ITEMS incl. `_identity_values` using a looser claim-shape rule (`"value" in node`) than the two production walkers (`CLAIM_KEYS <= set(node)`) — unreachable today, loud if reached. PASS b: REQUIRED empty, APPROVE, 18 mutations all killed. It verified the corrected NOTES `_refuse_unknown` paragraph clause by clause against extract.py line numbers, confirmed the 24->20->10+10 arithmetic is re-derivable by measuring the emitted schemas, and confirmed the 5-of-221 duplication is genuinely gone. THREE FACTUAL ERRORS IT FOUND IN THE PR, ALL FIXED: "Twenty-six open items" against DEBT's 27 — the commit that added D27 did not re-derive the count in the file quoting it, D27's own pattern a third time; "up from 626 at the start of the phase" — orchestrator measured `main` (b15e783) in a throwaway worktree at 485, since 626 was the mid-phase iteration-2b figure; and "A DATABASE_URL was never available, so no batch has run against stored papers at all", contradicted by the live run sixteen lines earlier in the same document, which cannot have happened without a database. Pass b also showed the removed "twenty reviewer passes" was standing on LOOP_LOG's own "eighteen", itself 9x2 and not re-derivable, since iteration 1 was a single full-diff review and iteration 2's review never ran — so removing the count rather than re-deriving it was more right than the commit knew. PASS b RULED D27 DOES NOT BELONG IN DEBT.md AS WRITTEN: the file's first line says "Not decisions, not rationale, not loop history" and D27 trips all three, its decision is explicitly NOT made (unlike D11, which was reclassified by arguing the decision was), and "decides: Phase 3" names a when where every sibling names an actor. Where it goes is the human's call. Remaining known items: the PR still restates NOTES' "seven decorative cases" and "71 entries, 107 mutations" in the same paragraph as the rule; D27's own "has worked eight times" is a bare count in the entry whose subject is bare counts going stale; D20 restates NOTES without a pointer; and the PR never says the HUMAN authorised the cap extensions and the exit-condition change, which a merge reviewer needs since CLAUDE.md still states zero REQUIRED and a cap of 5.
